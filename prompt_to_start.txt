Start iterative persona workflow (cap=3).

Requirement: Build the POC “Tenant Shell + Access Guards” (Sandiwa-OS.txt) — DOCS/CONCEPT FIRST.

Requirements-Ready Gate (do not proceed until all are available):
A) Ideation/Concept inputs
- UX concept notes (what the user sees on: platform landing, barangay landing, brgy admin shell)
- Key user journeys (at least: open on barangay host unauthenticated, login as brgy staff, navigate protected module)

B) Non-project assets / look-and-feel
- Provide at least 1 sample look-and-feel reference per UI shell:
  - Public “Impact” landing shell
  - “Makabayan” admin dashboard shell
  - (Optional) a light/dark contrast or accessibility note if relevant
- Provide any screenshots/icons/UI panel examples you want matched

C) Enforceable design constraints
- Art direction must be translated into tokens/component rules (not vague “vibe” only)
- No arbitrary colors/spacing outside tokens

Only after the above is present:
Implement POC architecture skeleton (Nuxt 4 + Pinia + localStorage, SPA/static):
1) Tenant resolution via hostname (client-side only for SPA/static POC)
2) Pinia tenant store + feature toggles persisted/seeded in localStorage
3) Mock auth/roles for platform admin vs brgy staff
4) Route guarding + UI mode switching (Impact vs Makabayan)
5) Security expectations for the POC:
   - prevent tenant leakage in UI/state
   - treat localStorage auth as untrusted; enforce role/module access in guards/UI logic
   - validate/safely render any user-editable content in the UI

Missing inputs behavior:
If any “Requirements-Ready Gate” item is missing, stop the loop early and ask you in this shape:
## Blocked — Missing Inputs
- what is missing:
- why it blocks Design/Frontend/Security:
- question for user:

Stop criteria:
- smoke: skipped (no runnable app yet)
- PASS only if Design + Security handoffs are internally consistent and no Critical security issues remain after Security review.